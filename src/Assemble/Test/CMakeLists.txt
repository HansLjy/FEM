# file(GLOB ASSEMBLE_TEST_SOURCE *.cc)

# add_executable(AssembleTest ${ASSEMBLE_TEST_SOURCE})

# include(GoogleTest)
# gtest_discover_tests(AssembleTest)

# target_link_libraries(AssembleTest Assemble)
# target_link_libraries(AssembleTest FEMUtility)